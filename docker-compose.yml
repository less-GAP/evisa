version: '3'

services:
    app:
        build: .
        working_dir: /var/www
        user: ${MY_UID}:${MY_GID}
        command: 'bash -c "composer install && php artisan octane:start  --server=swoole --host=0.0.0.0 --port=80"'
        volumes:
            - ./:/var/www
            - ./database.sqlite:/var/www/database.sqlite
            - ./public/index.php:/var/www/public/index.php
        networks:
            - apisix
    nginx:
        build: ./docker/nginx
        user: ${MY_UID}:${MY_GID}
        container_name: evisa
        depends_on:
            - app
        volumes:
            - ./public:/var/www/public
            - ./docker/nginx/conf.d/server.conf:/etc/nginx/conf.d/server.conf
            - ./storage/uploads:/var/www/public/uploads
        environment:
            NGINX_NO_CACHE: ${NGINX_NO_CACHE}
            NGINX_PAGESPEED: 1
            NGINX_PAGESPEED_IMG: 1
            NGINX_PAGESPEED_JS: 1
            NGINX_PAGESPEED_CSS: 1
        restart: on-failure
        networks:
            - apisix
networks:
    apisix:
        external: true



